Import-Module -Name Terminal-Icons
Import-Module PSReadLine
Set-PSReadLineOption -PredictionSource History
Set-PSReadLineOption -PredictionViewStyle InlineView

# Navigation helpers (functions because Set-Alias doesn't accept parameters)
function .. { Set-Location .. }
function ... { Set-Location ../.. }

# Open current folder in explorer
function o { explorer.exe . }

# Git status shortcut function
function gs { git status }

# Clear terminal shortcut alias is fine
Set-Alias cls Clear-Host

Import-Module PSFzf
Import-Module posh-git

# PSReadLine key bindings for history search
Set-PSReadLineKeyHandler -Key Ctrl+R -Function HistorySearchBackward
Set-PSReadLineKeyHandler -Key Ctrl+S -Function HistorySearchForward

# PSReadLine colors (removed invalid ConsoleInformation key)
Set-PSReadLineOption -Colors @{
    Command            = '#D9A6FF'
    Parameter          = '#F5C2E7'
    String             = '#ABE9B3'
    Operator           = '#F4DBD6'
    Variable           = '#F2CDCD'
    Comment            = '#6E6A86'
    Keyword            = '#F28FAD'
    Type               = '#96CDFB'
    Number             = '#F8BD96'
    Member             = '#89DCEB'
    Emphasis           = '#F2CDCD'
    Selection          = '#181825'
    Error              = '#F28FAD'
}


# Oh-My-Posh standalone CLI initialization
# Make sure Oh-My-Posh CLI is installed and in your PATH
oh-my-posh init pwsh --config "C:/Users/anon/Documents/.poshthemes/catppuccin-mocha.omp.json" | Invoke-Expression

# Optional: clear the screen on new terminal launch
Clear-Host

# Custom startup message with valid console colors
Write-Host "☕ Welcome back, $env:USERNAME! Today is $(Get-Date -Format 'dddd, MMMM dd yyyy, HH:mm')" -ForegroundColor Magenta
Write-Host "🚀 Have a productive session with Catppuccin Mocha vibes!" -ForegroundColor Green

# Define the path to your ASCII art file
$asciiArtFile = "C:/Users/anon/Documents/6ix9ine_asciii.txt"

# Check if the file exists before attempting to read it
if (Test-Path $asciiArtFile) {
    # Read the contents of the file and display them
    Get-Content $asciiArtFile
}
